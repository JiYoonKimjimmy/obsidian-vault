
## 캠페인 프로모션 대량 처리 Batch 구현 계획 리뷰

- "4.1 InitStep - Tasklet 패턴" 문서 수정
	- `reservation_at` 매개 변수는 옵셔널로 없다면 현재 시간 기준으로 DB 조회할 수 있도록 로직 구현 필요
- "4.3 Reader - JdbcPagingItemReader" 문서 수정
	- Partitioner 또는 `partition_key` 조건으로 데이터 조회하는 로직 구현 필요

- `application-infrastructure-kafka.yml` 를 참고하여 다른 토픽 설정과 유사하게 토픽 설계 필요
- 신규 토픽 설정 정보 `EventProperty` 프로퍼티 추가 필요
- `PocketEventType` enum 클래스에 신규 토픽 설정에 대한 enum 추가 필요
- `EventProviderService.getEventTopic()` 함수에 case문 추가 필요

- campaignPromotionVoucherPublish: campaign-promotion-voucher-publish
- campaignPromotionPointPublish: campaign-promotion-point-publish

---

- ArgumentCaptor 학습 필요